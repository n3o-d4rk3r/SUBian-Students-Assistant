<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="bd.edu.sub.subian">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatsApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".community.ProfileActivity" />
        <activity
            android:name=".Activities.ChatFragActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar" />
        <activity
            android:name=".community.PostActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar" />
        <activity
            android:name=".wtslider.SplashActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wtslider.ActivityWtslider"
            android:theme="@style/Theme.ChatsApp.NoActionBar" />
        <activity android:name=".Activities.GroupChatActivity" />
        <activity
            android:name=".Activities.ChatActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar" />
        <activity android:name=".Activities.SetupProfileActivity" />
        <activity
            android:name=".Activities.OTPActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".Activities.PhoneNumberActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/Theme.ChatsApp.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_send" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
    </application>

</manifest>